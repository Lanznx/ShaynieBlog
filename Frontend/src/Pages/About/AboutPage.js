import React from "react";
import Grid from "@mui/material/Grid";
import { Typography } from "@mui/material";

export default function About() {
  return (
    <Grid container spacing={3}>
      <Typography variant="h9">哈囉，這裡是 About</Typography>
    </Grid>
  );
}
